#server.port=8443

# Disabling SLL connection to database
spring.datasource.url = jdbc:mysql://localhost:3306/coursedatabase?useSSL=false
spring.datasource.username = vadim
spring.datasource.password = gresua

#spring.datasource.url = jdbc:mysql://us-cdbr-iron-east-04.cleardb.net:3306/heroku_b714bc5a91769ff?reconnect=true
#
#spring.datasource.username = be875fd7939285
#spring.datasource.password = 1624c5ca

spring.datasource.driver-class-name = com.mysql.jdbc.Driver
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.tomcat.test-while-idle = true
spring.datasource.tomcat.validation-query = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = update
spring.jpa.generate-ddl = true

#Redis data

#spring.security.user.name=user
#spring.security.user.password=password

#spring.redis.host=127.0.0.1
#spring.redis.port=6379
#spring.redis.password=

#spring.session.store-type=redis
#server.servlet.session.timeout= # Session timeout. If a duration suffix is not specified, seconds will be used.
#spring.session.redis.flush-mode=on-save # Sessions flush mode.
#spring.session.redis.namespace=spring:session # Namespace for keys used to store sessions.

